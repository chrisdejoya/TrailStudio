{
  "name": "trailstudio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild js/entry.js --bundle --sourcemap --format=iife --target=es2018 --outfile=js/app.bundle.js",
    "update-three": "npm install three@latest --save-dev && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "three": "^0.185.1"
  }
}
