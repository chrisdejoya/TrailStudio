{
  "name": "trailstudio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "check": "npm run build",
    "update-three": "npm install three@latest --save-dev && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "prettier": "^3.6.2",
    "three": "^0.185.1"
  }
}
